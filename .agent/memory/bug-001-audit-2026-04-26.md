# BUG-001 audit (implementation review) — 2026-04-26

Lead engineer audit per owner request. ClickUp MCP was not available in the agent toolset; task IDs (86excud7h / 7p / 80) are referenced here only.

## Subtask 86excud7h — Fingerprint dedup + Postgres history (P0)

| Item | Verdict | Notes |
|------|---------|--------|
| `titleFingerprint.ts` trigram similarity | OK | Character trigrams, stop-word strip, Jaccard-like overlap `|∩|/max(|A|,|B|)`. |
| Threshold 0.35 | ⚠️ (documented) | Lower bar than 0.55 → more pairs treated as duplicates; tradeoff documented in `titleFingerprint.ts`. |
| Dual-check file vs Postgres | ⚠️ → fixed | Previously Postgres skipped `loadPostHistoryDedupSnapshot` for URL/fingerprint checks; only URL `claimArticle` ran. **Fix:** always load snapshot and run `isArticlePostedInHistory` before claims (`newsFiltering.ts`). |
| `INSERT … ON CONFLICT DO NOTHING` | OK | Matches `uq_post_history_normalized_url`; `RETURNING id` correctly interprets row count. |
| Migration SQL | OK | Aligns with `ensurePostHistorySchema` (table + `normalized_url` / `status` + unique index). |
| NUL bytes | OK | `sanitizeText` strips `\0` in `postHistory.ts` on claim/record paths. |
| Race windows | ⚠️ | File store remains multi-writer unsafe (expected). Postgres: URL races covered by claim; fingerprint vs history covered after snapshot fix. |

## Subtask 86excud7p — Diverse selection + race (P1)

| Item | Verdict | Notes |
|------|---------|--------|
| `selectBestArticle` `'diverse'` | OK | Top-3 random branch; `'diverse'` passed from `pipelineRun.ts` L128. |
| `'random-top-3'` | OK | Replaced by `'diverse'` (see `decisions.md`). |
| Abort on lock loss | ⚠️ | `runWithLockHeartbeat` aborts signal and throws after `fn` if lock lost (`schedulerLock.ts`). `runPipeline` checks `signal?.aborted` only immediately before publish (L215–217); long steps may continue until completion, then wrapper throws. |
| `REDIS_URL` | OK | Required for `getRedisClient()` (lock); GNews cache skips Redis when unset (`newsService.ts`). |
| Remaining races | ⚠️ | See file history; Postgres claim + snapshot dedup mitigates URL/fingerprint issues. |

## Subtask 86excud80 — Hard filter + empty-slide guard (P2)

| Item | Verdict | Notes |
|------|---------|--------|
| Empty-slide guard before render | OK | `hasEmptySlides` → `throw` before `renderMedia` (`pipelineRun.ts` L165–180). |
| Repetitive topic | Hard skip | `sameTopicRecentCount >= threshold` filters out in `filterAndRankArticles` (not soft penalty only). Penalty in `scoreArticleRelevance` still exists but same-topic count passed as 0 after filter. |
| AI empty / corrupt | ⚠️ | Empty `data` blocked; corrupt shapes depend on `generateContent` / manifest validation — not fully generalized. |

## Cross-cutting

| Item | Verdict | Notes |
|------|---------|--------|
| Tests | Improved | Postgres dedup-before-claim test in `newsFiltering.test.ts`; empty-slide test in `pipelineRun.test.ts`; `postHistory.postgres.test.ts` for claim/upsert. |
| RSS-01 (XXE) | ⚠️ | `rss-parser` uses `xml2js`; explicit `trim`/`normalize` added. Classic external-entity XXE is low risk in typical Node `sax` usage; no full DTD hardening added. |
| SEC-03 (GNews URL) | OK | `safeUrl` / `safeSearchUrl` redact `apikey` in error logs (`newsService.ts`). Logger redacts keys named `key`, etc. |
| SRV-01 Docker | OK | `npm run build` → `start:prod` runs `node dist/server.js` (`Dockerfile` L47, L52; `package.json`). |
| `npm test` | Not verified here | Agent shell could not run vitest in this session; run locally: `npx vitest run`. |

## Code changes applied in this audit pass

1. `newsFiltering.ts` — Postgres path uses same URL + fingerprint dedup snapshot as file mode.  
2. `schedulerLock.ts` — Comment updated to match abort/throw behavior.  
3. `titleFingerprint.ts` — Documented 0.35 vs 0.55 tradeoff.  
4. `rssService.ts` — `xml2js: { trim, normalize }` on `Parser`.  
5. Tests — `newsFiltering` postgres dedup case; `pipelineRun` empty-slide case; `postHistory` mock completes `recordPost` / `isPostgresPostHistory`.
