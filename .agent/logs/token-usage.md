# Token Usage Log

**Purpose:** Token usage metrics when zone >= YELLOW.

---

*(Entries will be added during sessions. Format:)*

```
## Turn {N} | {MODE} | {ZONE}
- Tokens: {used}/{BUDGET_LIMIT} ({utilization:.0%})
- Freed: {freed} via {method}
```
